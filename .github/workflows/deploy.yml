name: Deploy Vite to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20      # без cache, чтобы не требовался lock

      - name: Install deps
        run: npm install

      - name: Build
        run: npm run build

      # 👇 Диагностика: убеждаемся, что Vite переписал /src/main.tsx на /assets/*.js
      - name: Sanity-check built index.html
        run: |
          echo "----- FIRST 40 LINES OF dist/index.html -----"
          head -n 40 dist/index.html
          echo "----- GREP check -----"
          if grep -q "/src/main.tsx" dist/index.html; then
            echo "ERROR: dist/index.html всё ещё ссылается на /src/main.tsx -> сборка неверная"; exit 1;
          fi
          if ! grep -q "/sales-script-graph/assets/" dist/index.html; then
            echo "ERROR: в dist/index.html нет ссылок на /sales-script-graph/assets/*.js"; exit 1;
          fi
          echo "OK: сборка корректная."

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
